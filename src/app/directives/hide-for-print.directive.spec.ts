import { HideForPrintDirective } from './hide-for-print.directive';

describe('HideForPrintDirective', () => {
  it('should create an instance', () => {
    const directive = new HideForPrintDirective();
    expect(directive).toBeTruthy();
  });
});
